name: sync-check
on:
  push:
  pull_request:
jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generated surfaces match core/
        run: bash scripts/sync.sh --check
