# Python Style Guide
- All functions must have type hints.
- Function names use snake_case; no abbreviations (e.g. `calculate_total`, not `calc_tot`).
- Never use bare `except:`; catch specific exceptions.
